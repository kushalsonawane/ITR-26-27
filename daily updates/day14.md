# Day 14: Sets, Maps & Chaining

- Learned standard built-in iterables: `Sets` and `Maps`.
- Practiced object copying using `Object.assign`.
- Studied JavaScript optional chaining (`?.`) to avoid undefined exceptions.
